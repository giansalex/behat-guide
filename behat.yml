default:
    suites:
        default:
            contexts:
                - FeatureContext:
                    - Awesome
                - WebFeatureContext
    extensions:
        Behat\MinkExtension:
            base_url: "https://github.com"
            goutte: ~